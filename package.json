{
  "name": "busirocket-rules",
  "version": "1.0.0",
  "private": true,
  "description": "Cursor rules template - reusable project rules",
  "scripts": {
    "format": "prettier --write .cursor/rules/**/*.mdc",
    "format:check": "prettier --check .cursor/rules/**/*.mdc"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  },
  "packageManager": "yarn@4.12.0"
}
