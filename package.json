{
  "name": "busirocket-rules",
  "version": "1.0.0",
  "private": true,
  "description": "Cursor rules template - reusable project rules",
  "scripts": {
    "format": "prettier --write .cursor/rules/**/*.mdc && prettier --write README.md",
    "format:check": "prettier --check .cursor/rules/**/*.mdc && prettier --check README.md"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  },
  "packageManager": "pnpm@10.29.1"
}
